# Phase v9.1: SessionHeader 响应式工具栏 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-03
**Phase:** v9.1-SessionHeader 响应式工具栏
**Areas discussed:** 响应式断点, 活跃状态, 菜单清理, 架构模式, Tab 栏位置, 返回聊天导航, 范围影响

---

## 响应式断点

| Option | Description | Selected |
|--------|-------------|----------|
| lg (1024px) — 保持现状 | 当前断点，大多数平板横屏开始显示图标 | ✓ |
| md (768px) | 更小断点，平板竖屏就能看到图标 | |
| 始终显示 | 所有屏幕都显示图标，不隐藏 | |

**User's choice:** lg (1024px) — 保持现状
**Notes:** 与当前 `hidden lg:flex` 一致，无需调整

---

## 活跃状态

| Option | Description | Selected |
|--------|-------------|----------|
| 是，高亮显示 | 图标变为高亮色 var(--app-link)，用户清楚当前所在功能 | ✓ |
| 否，统一样式 | 点击跳转后图标保持默认样式 | |
| 背景色区分 | 点击后图标加背景色 | |

**User's choice:** 是，高亮显示
**Notes:** 使用 var(--app-link) 颜色

---

## 菜单清理

| Option | Description | Selected |
|--------|-------------|----------|
| 仅保留会话管理 | "..."菜单只保留：重命名、归档/删除 | ✓ |
| 保留其他工具入口 | 保留 Changes/Timeline/Undo/Whiteboard | |
| 全部清理 | 桌面端隐藏所有内容 | |

**User's choice:** 仅保留会话管理
**Notes:** 桌面端菜单简洁化

---

## 架构模式

| Option | Description | Selected |
|--------|-------------|----------|
| 共享 Header | SessionHeader 升级为共享布局组件，包裹所有子页面。子页面不再有自己的 header。 | ✓ |
| 仅修菜单重复 | 保持现有架构，仅修复"..."菜单重复问题 | |
| 双层 Header | 主页面全图标，子页面精简版 | |

**User's choice:** 共享 Header
**Notes:** 这是一个架构层面的重大决策，改变了当前子页面各自独立 header 的模式

---

## Tab 栏位置

| Option | Description | Selected |
|--------|-------------|----------|
| 内容区内部 | 共享 Header 只放工具图标和会话信息，tab 栏在内容区顶部显示 | ✓ |
| Header 内双行 | 工具图标行下面再加一行 tab 栏 | |
| 替换模式 | 进入子页面时工具图标行变为该页面的 tab 栏 | |

**User's choice:** 内容区内部
**Notes:** 简洁的 Header，复杂内容留在内容区

---

## 返回聊天导航

| Option | Description | Selected |
|--------|-------------|----------|
| 点击已激活图标切换回 | 点击已高亮的 Git 图标再灭回到聊天视图 | ✓ |
| 添加聊天图标 | 在工具图标组最前面加一个"聊天"图标 | |
| 返回按钮上下文感知 | 共享 Header 的返回按钮在子页面时回到聊天 | |

**User's choice:** 点击已激活图标切换回
**Notes:** Toggle 行为，类似 VS Code 侧边栏

---

## 范围影响

| Option | Description | Selected |
|--------|-------------|----------|
| v9.1 做架构 + 基础样式 | v9.1 完成共享 Header 架构改造，v9.2 仅微调样式统一 | ✓ |
| 拆分到 v9.2 | v9.1 只修菜单重复，共享 Header 全部放 v9.2 | |

**User's choice:** v9.1 做架构 + 基础样式
**Notes:** v9.1 一步到位

---

## Claude's Discretion

- 共享 Header 的具体组件结构（是否拆分为 SessionHeader + SubPageLayout）
- 路由层级重构的具体实现方式（layout route vs wrapper component）
- 过渡动画的细节
- Tab 栏在内容区的具体样式调整

## Deferred Ideas

None — discussion stayed within phase scope
