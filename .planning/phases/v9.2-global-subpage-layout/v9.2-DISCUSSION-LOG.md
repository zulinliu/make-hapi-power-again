> ⚠️ **v0.12.0 功能精简 (2026-06-04)**: Changes/Timeline/Undo/Whiteboard/Orchestration 功能已删除。本文档中的相关规划内容已过时，请参考 STATE.md "当前活跃功能清单"。

# Phase v9.2: 全局子页面布局统一 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-03
**Phase:** v9.2-全局子页面布局统一
**Areas discussed:** CSS 变量替换范围, SubPageLayout 组件设计, 内容宽度约束, CSS 映射策略, Tab 样式统一

---

## CSS 变量替换范围

| Option | Description | Selected |
|--------|-------------|----------|
| 仅子页面路由 + Git 组件 | 最小改动范围，共享组件留到后续 | |
| 扩展到共享 UI 组件 | 子页面路由 + Git 组件 + ContextMenu + FileDialogs + ConfirmDialog + button.tsx | |
| 全量替换 | 所有 --hp-* 一次性替换为 --app-* 等效变量（含 Editor、DiffView、SessionList、typography.css） | ✓ |

**User's choice:** 全量替换
**Notes:** 最彻底方案，消除所有 --hp-* 残留

---

## SubPageLayout 组件设计

| Option | Description | Selected |
|--------|-------------|----------|
| 最小容器（仅包裹） | 提供 max-w-content + padding，tab 栏和工具栏各页面自行渲染 | |
| 带 Tab 栏插槽 | 容器 + 统一 Tab 栏渲染（接受 tabs/activeTab/onChange props） | ✓ |
| 不需要组件，统一样式即可 | 保持各页面独立但视觉一致 | |

**User's choice:** 带 Tab 栏插槽
**Notes:** 统一 Tab 渲染逻辑，减少各页面重复代码

---

## 内容宽度约束

| Option | Description | Selected |
|--------|-------------|----------|
| 统一 max-w-content | 所有子页面使用 max-w-content（960px 默认，xl+ 90%/1200px） | ✓ |
| 代码页面全宽，其他限制宽度 | 代码相关页面全宽，其他页面 max-w-content | |
| 全部全宽 | 所有页面撑满可用空间 | |

**User's choice:** 统一 max-w-content
**Notes:** 与文件页标准一致，阅读体验好

---

## CSS 映射策略

| Option | Description | Selected |
|--------|-------------|----------|
| 新增 --app-* 变量补全映射 | 在 index.css 新增 success/warning/primary-subtle/text-secondary 变量 | ✓ |
| 复用现有变量（git 颜色等） | --hp-success → --app-git-staged-color，不新增变量 | |
| 保留设计令牌层 + 新增变量 | 保留 --hp-* tokens.css/typography.css，组件中替换为新增 --app-* | |

**User's choice:** 新增 --app-* 变量补全映射
**Notes:** 语义明确，后续维护简单。设计令牌层（tokens.css/typography.css）保留不变

---

## Tab/筛选栏样式

| Option | Description | Selected |
|--------|-------------|----------|
| 统一 Tab 下划线 | 所有页面统一为下划线 Tab 样式（文件页标准） | ✓ |
| 保留混合样式 | Git/Extensions Tab 下划线，Changes/Timeline/Undo pill 按钮 | |
| 统一 pill 按钮 | 所有页面改为 pill 按钮样式 | |

**User's choice:** 统一 Tab 下划线
**Notes:** Changes/Timeline/Undo 的 pill 按钮改为 Tab 栏

---

## Claude's Discretion

- SubPageLayout 的具体 props 接口设计（是否包含 toolbar/action slot）
- 新增 --app-* 变量的具体值（配色风格）
- 各页面迁移到 SubPageLayout 的具体实现方式
- Git 子组件 inline style 转 Tailwind 类名的方式
- Tab 下划线切换动画细节

## Deferred Ideas

None — discussion stayed within phase scope
